```c++
#include <bits/stdc++.h>
using namespace std;
int n, m, k;
string s;
map<string, string> mp;
void split(string s, string delimiter){
	int pos;
	string token;
	vector<string> v;
	
	for(int i = 0; i < s.size(); i++){
		
	}
}
int main(){
	cin >> n >> m;
	for(int i = 0; i < m; i++){
		getline(cin, s);
	}
	cin >> k;
	for(int i = 0; i < k; i++){
		getline(cin, s);
	}
}
```
cf. split 필요없나..?