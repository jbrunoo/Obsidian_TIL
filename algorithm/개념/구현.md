구현은 쉬운 알고리즘. 말그대로 문제를 구현.
ex) 배열을 회전, 스택에 넣어라 -> rotate 함수, stack.push 등


```c++
// 앞에서 3번째 문자열 출력
// 거꾸로 출력
// umzunsik 추가 출력

#include <bits/stdc++.h>
using namespace std;
string dopa = "umzunsik";
int main(){
	cout << dopa.substr(0, 3) << "\n";
	string dopa2 = dopa;
	reverse(dopa2.begin(), dopa2.end());
	cout << dopa2 << "\n";
	cout << dopa + dopa << "\n"; // dopa + " umzunsik"
	
	return 0;
}
```


```c++
#include <bits/stdc++.h>
using namespace std;
int main(){
	int a[9];
	for(int i = 0; i < 9; i++){
		cin << a[i];
		vector<int> v;
		
		for(int i = 0; i < 3; i++)v.push_back(a[i]);
		
		do{
			if(sum(a) == 100){
				
			}
		}while(next_permutation(v.begin(), v.end()));
		
		
		
	}
	
	vector<int> v;
	for(int i = 0; i < )
	
}


int main(){
	int a[3] = [1, 2, 3];
	int n = 3, r = 3;
	vector<int> v;
	
	for(int i = 0; i < 3; i++)v.push_back(a[i]);
	
	do{
		
	}while(next_permutation(v.begin(), v.end()));
}



// 조합 9명중 2명을 뽑아서 2명의 합을 전체에서 뺏을 때 100이 나오면 그 둘을 빼고 나머지 정렬해서 출력 

```