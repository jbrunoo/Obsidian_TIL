
```c++
#include <bits/stdc++.h>
using namespace std;
int main(){
	int a[] = {1, 2, 3, 4, 5};
	vector<int> v;
	int cnt = 0;
	
	for(int i = 0; i < 5; i++)v.push_back(a[i]);
	
	do{
		for(int i = 0; i < v.size(); i++){
			cout << v[i] << " ";
		} cnt++; cout << cnt << "\n";
	}while(next_permutation(v.begin(), v.end()));
	cout << cnt;
	return 0;
}
```

